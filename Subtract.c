#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
int main()
{
    int a, b;
    printf("Enter 2 numbers:");
    scanf("%d",&a);
    scanf("%d",&b);
    int c = a-b;
    printf("%d",c);
    return 0;
}